{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user",
        "siswa"
      ]
    },
    "is_approved": {
      "type": "boolean",
      "description": "Whether the user has been approved by an admin to access the app",
      "default": false
    },
    "photo_url": {
      "type": "string",
      "description": "Profile photo URL",
      "format": "uri"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}